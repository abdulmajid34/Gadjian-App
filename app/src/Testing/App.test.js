import App from '../App'
import PersonelList from '../pages/PersonelList';


// const Button = ({ onclick, Next }) => (
//     <button onclick={onclick}>{Next}</button>
// ) 

// test('renders learn react link', () => {
//     const handleClick = jest.fn()
//     render(<Button onclick={handleClick}>Next</Button>);
//     fireEvent.click(screen.getByText(/Next/i))
//     expect(handleClick).toHaveBeenClledTimes(1)
  
// });

// test('link router testing', () => {
//     render(<App />);

//     const linkPersonelEmployee = screen.getByText(/PersonelList/)

//     expect(linkPersonelEmployee).toHaveTextContent()
// })
